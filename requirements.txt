# Runtime dependencies. Install into the shared venv one directory up:
#   ..\.venv\Scripts\python.exe -m pip install -r requirements.txt
pygame==2.6.1
numpy>=2.0

# Build only — needed to produce the .exe, not to play from source.
pyinstaller>=6.0
